# ROADMAP — comment-distillery 长期规划

> 制定：2026-09-26（第七战收口后）
> 前置阅读：`HANDOFF.md`（冷启动）· `docs/retrospective.md`（七战沉淀）
> 与旧文档的关系：`cases/notes/comment-distillery开源冲刺路线图.md`（M0–M4）聚焦**开源分发**，其 M1/M2 内容并入本文 P3/P4；**两份冲突时以本文件为准**。

---

## 0. 定位锚（不变）

- **只做最后一跳**：群体文本 → 带引用、带盲区说明、带反例对冲的厚指南。
- 不写爬虫（上游复用生态）· 不做情感饼图（L2 红海）· 不追 star 数字本身。
- 零依赖纯标准库是**设计约束**，不是偷懒——它保证任何环境一条命令跑通。

## 1. 现在停止什么（边界纪律）

- **停止新增抓取**。语料已足够：七战累计约 4.5 万条，其中第六 + 七战单个视频就 21,100 条。继续抓只会增加语料体积，不增加方法论增量。
- **停止"单战即升级为铁律"**（见 P2）。
- **不把 star 当目标**，只当分发是否奏效的观测指标。

---

## 2. 阶段规划

### P0 · 收尾与清债 ✅ 已完成（2026-09-26）

| 项 | 内容 | 状态 |
|---|---|---|
| 源侧 `.git` 副本 | 比对确认独有提交 0 后清理 | ✅ |
| HANDOFF 一致性 | HEAD `0f95ee9`、七战索引、CI 状态、leftover 行 | ✅ |
| CHANGELOG | 补第七战条目（含 3 项实证） | ✅ |
| 正卷交叉引用 | 卷首回填"3 处结论被增补卷修正" | ✅ |
| SKILL.md | 六战→七战 | ✅ |
| 沉淀件 | `docs/retrospective.md` | ✅ |

**验收**：CI 绿 + 三份文档（HANDOFF / CHANGELOG / SKILL）口径一致。

---

### P1 · 质量可信化 ✅ 已完成（2026-09-26）

**为什么排第一**：只能证明"引用是真的"，**证明不了"指南变好了"**。换模型 / 改 SKILL.md / 调参数后有没有变强，全靠感觉——七战攒的经验没有可回归的载体，会随会话蒸发。

| 交付物 | 状态 |
|---|---|
| `golden/cases/*.json` | ✅ 8 个场景，4 类（`citation` / `coverage` / `counterevidence` / `layering`） |
| `golden/graders.py` | ✅ 8 种规则类型；`citation_real` 复用 `scripts/verify_citations.py`，不重写 |
| `golden/run_golden.py` | ✅ `--replies` 离线判分（不绑任何 CLI/模型）/ `--baseline` / `--self-test` / `--compare` |
| `golden/replies/baseline/` | ✅ 8 份理想回复 |
| `tests/test_golden.py` | ✅ 12 个用例，CI 自动跑（全量 39 → 27 + 12） |
| CI 接入 | ✅ 新增两步：`--self-test` + `--baseline` |

**验收结果（实测）**：
1. baseline 跑分 **100.0 / 100**（8/8 满分），报告落盘 `golden/results/`；
2. **变异验证 31 个变异体全部被正确扣分**——删关键词必须扣分、插禁词与假引用 ID 必须归 0；
3. CI 三步 Python（3.10 / 3.12 / 3.13）均可跑（纯标准库）。

**踩到并记录的坑**：禁词是朴素字符串匹配，**否定语境会误伤**——baseline 写"绝不能**凭印象**补一个 ID"被判红线（语义完全正确）。已写进 `golden/README.md` 已知局限第 1 条。

**格式偏离**：场景集用 **JSON 而非 YAML**。AGI-Distiller 用 YAML，但本项目零依赖是硬约束，PyYAML 会破承诺——这个偏离必须保留。

**剩余**（P1 未覆盖的，已并入 P5）：判分器**不判推断质量**，见 `docs/retrospective.md` §7-3。

---

### P2 · 方法论去过拟合 ✅ 已完成（2026-09-26）

**问题**：SKILL.md 里多数规则只被 1 战验证过。第七战的"楼中楼必需"来自单个视频；"低赞长文过滤 pasta"来自单个评论区。个案被写成铁律会误导后续。

**交付物**

| 文件 | 内容 |
|---|---|
| `docs/rule-provenance.md` | 规则溯源台账：9 条铁律（≥2 战）/ 5 条待复现（仅 1 战）/ 7 条环境事实，每条附**复现判据** |
| `docs/overturned.md` | 被推翻结论台账：OT-1…OT-5 每条写明"曾经相信 / 被什么打穿 / 现在怎么做 / **再次推翻的条件**" |
| `scripts/check_rule_tags.py` | 机验 SKILL.md 无裸铁律，带 `--self-test` 变异验证 |
| `tests/test_rule_tags.py` | 6 个用例；CI 新增两步 |

**验收结果（实测）**：SKILL.md 规则区 **24/24 条目带溯源标记，裸铁律 0**；变异验证 **3/3 拦截**；全量回归 **45 tests OK**（39 → 45）。

**实际执行中的偏离**：ROADMAP 原写"仅 1 战的先进 `cases/notes/`，标注待复现"，实际为**保留在 SKILL.md 末尾独立的「待复现观察」章节**。理由：`cases/` 已被 `.gitignore` 排除，移进去等于让 skill 使用者完全看不到。已记入 `docs/rule-provenance.md` §4。

**顺带修掉的真缺陷**：首版判据把「输入」的三种进入方式、「6 步流水线」的编号条目都当规则误报 9 条；第二版又漏掉"文件没有「实施注意」章节时待复现区完全不检查"。两次都是靠**变异验证 + 反例测试**暴露的，不是靠肉眼审代码。

**停止条件**：无（常态维护项——每完成一战回到 `rule-provenance.md` §2 看有没有可升级的）。

---

### P3 · 分发（决定外部影响力）— 🟡 进行中

沿用冲刺路线图 M2，按 ROI 排序。**完整作战清单（含可直接复制的 PR 文案 / 帖子草稿）见 `docs/distribution.md`。**

| 优先级 | 动作 | 验收 | 状态 |
|---|---|---|---|
| P0 | `npx skills add` 适配（vercel-labs/skills） | 一条命令可安装 | ✅ **已完成**（`--list` 实测 Found 1 skill）；frontmatter 已改触发式 + `metadata.version` |
| P1 | 英文 README 第一屏放**真实输出片段** | 3 秒能看懂产出是什么 | ✅ **已完成**（含引用/反例/行动清单/自曝盲区四段真实产出） |
| P1 | GitHub 仓库元数据（description + topics） | `gh repo view` 可见 | ✅ **已完成**（description 已设 / MIT / topics **16 个**） |
| P1 | topics 检索词补齐 | 搜索可命中 | ✅ **已完成**（2026-09-26 补入 `agent-skills` / `claude-skills` / `skills` / `ai-agents`，12 → 16） |
| P2 | `cases/` 产物层公开 | 外部能看到真实案例 | ✅ **已完成**（9 md / 277 KB；原始语料按合规约束不公开） |
| P1 | awesome 列表 PR（ComposioHQ / VoltAgent / m-fyi） | 至少 1 个合入 | ⏳ **需你执行**（§1-B 有现成英文 PR 文案） |
| P2 | Reddit `r/ClaudeAI` / `r/LocalLLaMA` Show & Tell + 中文社区同步 | 各发一轮 | ⏳ **需你执行**（§1-C 有骨架） |
| P3 | 方法论长文（"Why comment sections are the most underrated dataset"） | 长尾搜索流量 | ⏳ 素材已就位（§1-D） |

**适配时确认的关键规范**：仓库根 `SKILL.md` 即构成单 skill 仓库、无需 manifest；⚠️ **绝不能在其旁边再建 `skills/` 目录**（会短路发现）——保持现状，不要重构成 `skills/comment-distillery/`。

**✅ topics 已补齐（2026-09-26）**：复核发现原有 12 个 topics 里的 `agentskills`（**无连字符**）与生态流通检索词 `agent-skills` **不等价**——等于白加。已直接调 API 补入 4 个（`agent-skills` / `claude-skills` / `skills` / `ai-agents`），现为 **16 个**。§1-A 的命令已标注"不必执行，仅存档"。

**停止条件**：若 3 个月内 awesome PR 全被拒、且社区零反馈 → **回到定位重新评估**，不继续堆渠道动作。

---

### P3.5 · 交付形态（桌面工具 + 官网） ✅ 已完成（2026-09-26）

**为什么做**：方法论再强，**装不上就传播不了**。此前只有「装进 Agent」和「命令行跑脚本」两条入口，前者要求会用 skills CLI、后者要求会开终端——把绝大多数非技术用户挡在门外。而这个项目的最佳用户（运营 / 产品 / 内容研究者）恰恰是终端恐惧率最高的一群。

| 项 | 内容 | 状态 |
|---|---|---|
| 桌面 exe | Windows 单文件（约 11 MB，免安装）：① 采集 B站评论（含楼中楼）② 预处理 ③ 导出蒸馏包 ④ 引用机验 | ✅ `dist/comment-distillery.exe` |
| 官网 | 单文件静态页（`site/index.html`），内容全部取自仓库真实数据，GitHub Pages 发布 | ✅ |
| 构建可复现 | `scripts/make_icon.py`（标准库手写 ICO）+ `scripts/build_exe.py`（PyInstaller）+ CI 工作流真跑一遍打包与双冒烟 | ✅ |
| 零依赖承诺不破 | GUI 用 `tkinter`（标准库）；PyInstaller **仅构建期**依赖，不进运行期 | ✅ CI 守卫扩展到 `app/` |

**关键设计决策（不内置 LLM）**：exe 只做到「导出蒸馏包」，**蒸馏那一步交还用户自选的 AI**。理由有三——① 零 API Key、零调用成本；② 方法论直接引用仓库当前 `SKILL.md`，**不会随 exe 固化而过期**；③ 避免把"要用户填 Key"这件事强加给非技术用户。

**验收（已达成）**：
- `--selfcheck` 资源齐全性 PASS；
- `--selftest-gui` 真建窗口 PASS（不是"能 import"就算过）；
- `--selftest-run` 端到端真跑「预处理 → 打包 → 引用机验」PASS；
- 官网 Chrome 无头渲染核验：外链资源 0、锚点 0 缺失、整页 1400×6900 无布局塌陷。

**回滚方式**：删除 `app/` / `app_main.py` / `site/` / `scripts/build_exe.py` / `scripts/make_icon.py` / 两个新工作流即可，`scripts/` 与 `contrib/` 的改动均为**纯增量**（只加可调用入口，未改任何 CLI 签名，45 项原测试全绿）。

---

### P4 · 场景升维收尾 ⛔ **已决定不做**（2026-09-26 用户拍板）

| 动作 | 说明 | 风险 |
|---|---|---|
| `examples/` 真实脱敏数据集 | 现有 4 个视频语料去掉 UID 等标识后进仓库 | 脱敏不彻底 = 合规事故，**宁可不放** |
| 非 B 站输入适配器 | Reddit JSON / YouTube 导出 → CCF | 各平台字段差异 |

**验收**：至少 2 个非 B 站示例端到端跑通 + 脱敏数据集通过 PII 扫描。

**决定（2026-09-26）：不做。** 理由——脱敏不彻底即合规事故，而合成样例 `examples/sample_corpus.csv` 已能端到端演示全部流水线；非 B 站适配器同理，它真正的验证价值在「换平台后判据是否仍成立」，那属于 P5 研究问题，不是分发阻塞项。**重新考虑的触发条件**：出现 ≥2 位外部使用者明确卡在"没有非 B 站示例"。在此之前不投入。

> **附注（同日）**：P4 原本想要的"让外部看到真实案例"，已由 **`cases/` 产物层公开**部分满足——各战指南与设计文档公开，原始语料按 `docs/compliance.md` §2 不公开。因此本阶段的剩余价值进一步下降，维持"不做"。

---

### P5 · 研究性问题（长线，不设 deadline）

| 问题 | 为什么重要 |
|---|---|
| 跨平台对照（B站 vs YouTube 同题材） | Q1（广场/走廊比 31.9%/14.6%）是否平台特定？未验证就不可用 |
| 注意力漂移普适性（Q2 = 2.5×） | 目前**只有 1 个样本**，不能当常数 |
| 真·人工校验 loop | 现有 7 道门只卡形式与引用真实性，**不检验推断质量** |

**验收**：产出一份可公开的对照报告，明确回答"哪个判据是普适的、哪个是个案"。

---

## 3. 与 AGI-Distiller 的协同

两个项目共享"蒸馏"骨架，输入/产出正交（`E:\Program\AGI-Distiller`）：

| 方向 | 内容 |
|---|---|
| ⬅ 我们引入 | golden 评估集的三分离设计 + 离线判分模式（P1）；"≥N 次才升级"的沉淀门槛（P2）；rejected 归档机制（P2 台账） |
| ➡ 我们输出 | **引用机验（输入可溯源）**——它当前不需要（单篇源），一旦做多源合并蒸馏就会立刻需要；交付门禁清单 G1–G7 |
| 共用 | 分发渠道清单（7 平台兼容表 / marketplace / awesome 列表） |

---

## 4. 优先级总结（一句话版）

**先把"能不能证明自己变强"解决（P1），再谈让更多人知道（P3）。**
分发做得再好，如果方法论是过拟合的（P2 未做），传播出去的也是错的。

**附（2026-09-26）：P3.5 交付形态已插入 P3 之前完成** —— 理由很直接：分发渠道铺得再开，落地页却要求用户「先装 Node、再跑 npx skills add」，转化会在第一步就断掉。桌面 exe + 官网是**分发的前置条件**，不是分发的装饰。
