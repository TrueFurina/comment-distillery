# ── 语料数据：绝不提交真实用户数据 ──
# 本项目合规约束：不得二次分发原始语料。
# 只允许提交 examples/ 下的合成样例。
data/
corpus/
corpora/
raw/
out/
output/
distill/
*.corpus.csv
# ── 实战档案 cases/：只公开「产物层」（各战指南 + 设计文档），原始语料不公开 ──
# 依据 docs/compliance.md §2「不得二次分发原始语料」——顶部那条约束在此具体化。
# ⚠️ 曾用 `/cases/` 整目录忽略；现改为「按扩展名只放行 .md」，故规则必须锚定 /cases/** 前缀。
#    （另注意：不带前导斜杠的 `cases/` 会连带忽略任意层级的同名目录，实测吞过 golden/cases/）
/cases/**/*.csv
/cases/**/*.txt
/cases/**/*.json
/cases/**/*.py
/cases/**/*.svg
/cases/**/distill/
/cases/**/distill_full/
/cases/**/memory-snapshot/

# ── 凭证：绝不提交 ──
.env
.env.*
*.cookie
cookies.txt
*_cookies.json
*.session
credentials.json

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# ── 临时 ──
tmp/
*.tmp
*.log

# ── 会话 / 项目记忆（WorkBuddy 本地状态，不进仓库）──
.workbuddy/
# golden 跑分报告（随时可重生成，不进仓库）
golden/results/
