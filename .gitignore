# ── 语料数据：绝不提交真实用户数据 ──
# 本项目合规约束：不得二次分发原始语料。
# 只允许提交 examples/ 下的合成样例。
data/
corpus/
corpora/
raw/
out/
output/
distill/
*.corpus.csv
# 实战档案目录（各战语料 + 深度指南 + 中间产物），仅本地留存
# ⚠️ 必须锚定仓库根：写成 `cases/` 会连带忽略任意层级的同名目录（实测吞掉了 golden/cases/）
/cases/

# ── 凭证：绝不提交 ──
.env
.env.*
*.cookie
cookies.txt
*_cookies.json
*.session
credentials.json

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# ── 临时 ──
tmp/
*.tmp
*.log

# ── 会话 / 项目记忆（WorkBuddy 本地状态，不进仓库）──
.workbuddy/
# golden 跑分报告（随时可重生成，不进仓库）
golden/results/
