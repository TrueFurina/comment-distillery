---
name: comment-distillery
description: 把任何一群人对同一议题的自发文本——视频/帖子评论区（B站/YouTube/Reddit/知乎/小红书）、问卷开放题、访谈逐字稿、产品反馈、Issue/工单讨论、弹幕——蒸馏成带认知框架、共识vs争议、反例对冲、引用溯源、行动清单、盲区说明的厚干货指南。输入为统一语料 CSV（Canonical Corpus Format）。不做采集、不做情感饼图，只做 L3 建设性综合。
agent_created: true
---

# Comment Distillery（评论蒸馏器）

> **Turn a pile of crowd text into an evidence-cited action guide.**
> 把一堆群体文本，炼成一份有据可查的行动指南。

## 何时使用

- 用户手里有一份**群体文本语料**（评论/问卷/访谈/反馈/讨论记录），或给了视频链接，想要"有建设性的深度分析"，而不是情绪饼图 / 词云摘要。
- 用户要的是**能落手的认知结构**：共识是什么、争议在哪、反例是什么、该做什么、哪里可能是错的。
- 触发词：把评论整理成指南 / 评论区洞察 / 评论蒸馏 / 评论分析 / distill comments / comment to guide / 把问卷开放题整理一下 / 访谈记录提炼 / 用户反馈分析 / 群体文本分析。

**不适用**：单纯的频次统计、情感饼图、词云、舆情仪表盘（那属于 L2，市面已红海）。若用户明确只要这些，直接给结果，不必启动本流水线。

## 核心定位（差异化，务必牢记）

- **不做采集**：L1 抓取层已极度成熟。用户自备语料；若只给了视频链接，见「输入」第 3 条。
- **不做情感饼图**：L2 统计摘要红海，不重复。
- **只做 L3 最后一跳**：原始群体文本 → 结构化认知。这是空白且高价值的部分。
- **价值 = 把"一群人的七嘴八舌"炼成"可落手的认知框架 + 共识 vs 争议 + 反例对冲 + 引用溯源 + 行动清单 + 盲区诚实说明"**。

**三件套护城河（本 skill 与一切摘要工具的分水岭，任何情况下不许砍）**：
1. **反例对冲**——每条共识必须配反方论据；
2. **引用溯源**——每条带数据的结论必须标 `[源:ID|权重|…]`；
3. **盲区诚实说明**——幸存者/平台/算法/时间四类偏差主动披露。

## 输入

三种进入方式，**优先级从高到低**：

1. **统一语料 CSV（首选）**：符合 Canonical Corpus Format（见下表）。字段名容错，中英文/常见变体自动映射（完整别名表见 `docs/canonical-format.md`）。
2. **任意平台导出 → 换算**：把平台原始导出（CSV/JSON/Excel）映射成 CCF。只需要 `id` + `text` 两列就能跑，其余字段有则更准。
3. **只有视频链接（可选桥接）**：若当前 agent 能执行 Python，可用 `contrib/fetch_bilibili_comments.py`（纯标准库、零依赖）拉取并直接输出 CCF：
   - 用法：`python fetch_bilibili_comments.py <BV号或链接> [out.csv] [--roots-only]`
   - `--roots-only`：只拉一级评论、跳过楼中楼。**追求时效与稳定性时优先用它**——一级评论是观点主体，而子回复接口限流风险最高。实测同一视频：全量抓取 5 分钟仍未完成，roots-only 约 1 分钟拿满 1501 条。
   - ⚠️ `contrib/` 属**非核心可选件**，不在主仓支持范围内，合规责任由使用者自负（见 `contrib/README.md`）。

### Canonical Corpus Format（CCF，统一语料格式）

```csv
id,text,parent_id,is_reply,score,reply_count,created_at,author_id,source
```

| 字段 | 语义 | 必需 | 说明 |
|---|---|---|---|
| `id` | 唯一标识 | ✅ | 溯源锚点；省略时脚本按行号自动生成 `r{i}` |
| `text` | 正文 | ✅ | 空 / 纯符号 / 广告会被去噪 |
| `score` | 群体认可度（赞/赞同/upvotes/星） | 推荐 | 排序与权重依据，**不是真值**（见「高赞 ≠ 高信号」） |
| `reply_count` | 被回复数 | 推荐 | 用于筛「讨论型深水区」 |
| `parent_id` | 父条目 ID | 可选 | 层级/楼中楼结构 |
| `is_reply` | 是否回复 | 可选 | 决定「一级/回复」标记 |
| `created_at` | 时间 | 可选 | 时间切片偏差分析 |
| `author_id` | 作者 | 可选 | 多样性统计；**产出中不得外泄** |
| `source` | 来源标记 | 可选 | 多语料合并时区分来源 |

> 设计意图：**任何域归一成同一张表之后，下游 6 步流水线完全一致**。这是"平台中立"落到工程上的方式——不是写一堆适配器，而是把差异关在输入层。

### 支持的语料域（同类问题，同一解法）

| 域 | 典型来源 | 采集方式（用户自备） |
|---|---|---|
| 社区评论区 | B站、YouTube、Reddit、知乎、小红书、微博 | MediaCrawler、BilibiliCrawler、平台导出 |
| 用户研究 | 问卷开放题、访谈逐字稿、焦点小组 | 问卷星/腾讯问卷导出、飞书妙记、Otter |
| 产品反馈 | 应用商店评论、NPS 反馈、客服工单 | App Store/Google Play 导出、客服系统 |
| 开源社区 | GitHub Issue/PR 讨论、Discourse | GitHub API / 导出 |
| 团队协作 | 会议纪要、群聊记录、复盘文档 | 飞书/钉钉导出 |
| 学术质性 | 开放式问卷、田野笔记、二手文本 | 研究者自备 |
| 直播/弹幕 | 弹幕流、直播评论 | 平台工具 |

**共性抽象**：一群人对同一议题（或同一刺激）产生的、尚未结构化整理的文本集合。只要符合这个抽象，本流水线都适用；不符合（如单人日记、纯客观数据），不要硬套。

## 6 步流水线

1. **接入 Ingest**：拿到 CCF CSV。确认字段、规模、语料域。
2. **预处理 Prep**：运行 `scripts/prep.py <corpus.csv> <outdir>`。产出：
   - `all_comments.txt`——按 `score` 降序、每条带 `[ID | 权重 | 层级 | 复N]`、已去噪；
   - `stats.json`——规模/互动/时间样本/信号词；
   - `id_map.json`——`[{id, like, floor, reply}]`，**溯源的前提，务必保留**。
3. **通读与聚类 Read & Cluster**：**逐条通读** `all_comments.txt`（不是只看统计），按主题聚类，标注 共识 / 争议 / 独立观点。
4. **框架抽取 Synthesize**：抽认知框架、实操手册、护城河、路径选择、心态与组织政治等可落手结构。强制：
   - **反例对冲**：每条共识配反方论据；
   - **引用溯源**：每条带数据的结论后标 `[源:ID|权重N|层级]`，从 `id_map.json` 取；
   - **盲区诚实说明**：幸存者偏差、平台/人群偏差、权重（点赞）算法偏差、时间切片偏差。
5. **结构化输出 Emit**：默认**深度版**（见下）。
6. **验证 Verify**：
   - **⚠️ 强制质量门：引用真实性机验（不可跳过）**。AI 写长指南时**会生成看似合理但实际不存在的 ID**（幻觉引用）。写完必须跑：
     ```bash
     python scripts/verify_citations.py <guide.md> <corpus.csv> [more.csv ...] --field 评论ID
     ```
     规则：指南中所有 ID 引用与语料 ID 全集做差集，**差集必须为空才允许交付**（退出码 0）。
     实测战绩：某次 96 条引用中揪出 **1 条幻觉 ID**，替换为真实条目后复验通过。**没有这一步，指南的可信度就是不可验证的。**
   - 检查覆盖度（头部是否读到、长尾是否抽样）；文末附"请人工核对关键结论"提示（轻量人工校验钩子）。

## 输出档位

- **深度版（默认）**：完整厚指南——多章 + 行动清单 + 精华句 + 元方法论（金矿机制 / 可复用 SOP / 盲区）+ 方法与边界。带反例对冲、引用溯源、盲区说明。
- 标准版：章节 + 共识/争议梳理（省去反例/溯源细节）。
- 极简版：10 条核心结论 + 行动清单。

> **默认深度版**；仅当用户显式要求"简短/快看"时降级。不要自作主张削弱。

## 引用溯源规范

预处理已保留每条语料的 ID/权重/层级映射。抽取观点时，**每条带数据的结论后都附 `[源:ID|权重N|层级]`**，让指南从"AI 总结"变成"有依据的群体洞察"。

- ID 必须**逐字复制**自 `id_map.json` / `all_comments.txt`，**不许凭印象补写**。
- 引用条数少而真 > 多而假。**诚实的降级方式是减少引用，绝不是保留一个"看起来对"的 ID**。

## 合规前置（必须，写在产出开头或技能执行前提示）

- **用途限制**：仅限个人学习研究；**禁止**用于商业舆情监控、批量用户画像。
- **著作权**：原始文本归原作者与平台所有；产出仅为观点提炼，**不得二次分发原始语料**。
- **隐私**：产出中不得出现可识别个人身份的信息（真实姓名、账号、联系方式、精确位置）；`author_id` 只用于内部去重统计。
- **默认保守**：内置合理限速；**不提供任何绕过平台限制的选项**。

## 与现有工具的关系（不要重造）

- 采集层：MediaCrawler（多平台）、BilibiliCrawler（桌面 GUI）、bbc-skill（Agent CLI）、bilibili-api-python（库）、平台官方导出。
- 分析摘要层：市面 L2 工具止步饼图/词云/情感分布；本 skill 补 L3。
- 本 skill **只做最后一跳**，上下游全部复用生态。

## 首测数据集（跨话题泛化已验证）

已跑通五战，证明流水线跨话题、跨规模、跨语料形态泛化成立：

| # | 规模 | 语料 | 产出 | 额外验证 |
|---|---|---|---|---|
| 1 | 1504 条 | B站"AI 替代程序员"评论 | 《AI时代程序员生存进化干货指南.md》（11 章） | 流水线可行性 |
| 2 | 3425 条 | 《山》AI 电影评论 | 《《山》AI电影评论深度指南.md》（8 章 + 7 小节） | 两场核心争议专场（版权 / "手搓"语义） |
| 3 | 5052 条 | "清北 discouraged"评论（1501 一级 + 3551 楼中楼） | 《清北discouraged评论深度指南.md》（13 章） | **楼中楼捞出一级评论区缺失的 7 个议题，其中 4 条直接对冲前文结论**——反例对冲机制真实生效 |
| 4 | 8941 条 | 双视频合并（薪资 903 / 43 岁被裁 8038） | 《程序员生存双镜》评论深度指南.md（23 章） | **多语料合并产出单语料看不到的洞察**；**引用机验首次抓到幻觉引用**（96 条里 1 条）；低赞长文筛选规模化（7886 条筛出 486 条） |
| 5 | — | 本仓库 examples/ 合成样例 | 端到端回归 | CI 自动跑 |

## 实施注意

- **平台中立**：本 SKILL.md 不写死任何 agent 专属工具调用，可被 Claude Code / Codex / Cursor / WorkBuddy 等任意读取复用。在 WorkBuddy 内可调用 `present_files` / `show_widget` 增强呈现；其他平台可忽略该句。
- **通读必须真读全文**，不能只跑统计脚本就出结论。
- **⚠️ AI 会编造引用 ID（幻觉引用）**：本 skill 唯一一个"必须用脚本卡死"的质量门。写作时凭印象补 ID 是最高危动作——生成的 ID 格式完全正确、看起来毫无破绽，但语料里根本不存在。处理办法见第 6 步，**差集必须为空才算交付**。
- **超大语料（>5000 条）不要逐行读完**：用"头部通读 + 脚本精筛"两段式。头部按权重降序读到分值≈5–8（约 800–1000 条，覆盖全部主要议题）；尾部用脚本筛"低权重且长文"（如 权重≤8 且 字数≥120；实测 7886 条筛出 486 条），只精读其中最长/最相关的 30–40 条。**用约 1/6 的阅读量覆盖 95% 的信号**，并必须在附录诚实披露"未逐条读完的部分"。
- **⚠️ 高权重 ≠ 高信号（实测铁律）**：群体文本里，最高分往往被**零信息量的最大公约数**占据——词义科普、玩梗、附和、形式吐槽（用词/口音/长相/标题句式）。真正的认知框架常藏在**低分长文**里。
  - 实例：某视频排名第一（12317 赞）是"discouraged 是泄气的意思"，而**最重要的认知框架（两层 discouraged 的区分）只有 4 赞**；另一条关键区分"优绩是工具 vs 优绩主义是信仰"只有 2 赞。
  - 做法：先按权重降序通读头部锚定情绪基调，**再专门用脚本筛低分长文捞真信号**，两头都不能漏。
- **识别 meta 噪音**：若头部在争论**形式**（用词、口音、标题、长相）而非内容，说明该语料被跨圈层推流，目标受众的真观点被稀释在中腰部——此时更要在长尾找信号。这类噪音本身也可能是有价值的发现（它证明了某种群体状况），但必须标注为"噪音"而非当作观点。
- **楼中楼/深层回复值得单独处理**：子回复里常有顶层吵不出来的反驳与澄清（实测 3551 条楼中楼里捞到 7 个新议题，其中 4 条直接对冲了基于顶层得出的结论）。若采集受限，至少对 `reply_count>=3` 的讨论楼补一轮。
- **多份语料可合并分析**：同议题的多来源（不同视频/平台/时间）合并后，常能浮现单份看不到的结构性洞察（实测：两份"程序员薪资/中年被裁"语料合并后，才看出"初级岗位消失 → 成长通道断裂"这条闭环）。

### 环境坑（已踩过，别重蹈）

- **传给 Windows Python 的路径必须用 `C:/...` 而非 Git Bash 的 `/c/...`**：后者会被解析成 `C:\c\Users\...`，导致脚本"报成功"但产物落在错误位置、核验时找不到文件。
- **`SSL: UNEXPECTED_EOF_WHILE_READING` 中途断连通常是代理污染，不是被封**：Windows 上 urllib 默认 `getproxies()` 会读注册表系统代理（Clash 等失效后仍被沿用），表现为翻到第 N 页突然假死、数据只拿到一小半。修复：`urllib.request.build_opener(urllib.request.ProxyHandler({}))` **强制直连** + 请求级重试。实测：修复前翻到第 19 页断连只拿 360 条，修复后一次拿满 1501 条。
- **隔离 venv 里 `pip install` 卡在 `pyyaml`/setuptools 编译**（`cython_sources` / `_distutils_hack` 缺失）：是 setuptools 在中断升级里**损坏**了。修复 `pip install --force-reinstall --no-deps setuptools wheel`，并优先用镜像的二进制 wheel（`--only-binary :all:`）。**这也是本 skill 坚持零依赖纯标准库的原因之一。**
- **别自己写 bv2av**：网上多种版本（6 位/10 位、基 58/基 10、指数方向各异）极易错配，算出的 aid 会偏差几个数量级。直接调官方 `view` 接口取 `data.aid`。
- **用 heredoc（`python - <<'PY'`）写含 Windows 路径的内容会静默改写反斜杠**：写文件一律用编辑工具，不要用 heredoc。
